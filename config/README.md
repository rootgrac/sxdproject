# config — 配置表（Excel 源 + 导出 JSON）

> 状态：**待创建**（M0：导表工具）

规范（`docs/开发文档.md` §3.6）：

- 表头三行：字段名 / 类型 / 注释；首列为主键；枚举集中定义
- Excel 源与导出 JSON **均入库**；导出由 `tools/` 下的导表脚本生成
- 游戏内只读加载；数值改动走 PR + 战斗模拟器回归（≥1000 场校验胜率曲线）
- 规划子目录：`battle/`（属性、技能、效果器）、`stage/`（关卡）、`role/partner`、`item/` 等，随开发演进
