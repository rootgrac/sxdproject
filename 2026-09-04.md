# 2026-09-04

- 完成《神仙道》高清重制版手游开发文档：`docs/开发文档.md`（v0.1.0）+ `README.md`，涵盖项目概述与合规红线、功能清单（S1-S14，P0-P2 + 5 个批次）、技术方案（Cocos Creator 3.8+ TS / NestJS / MySQL / Redis / WS+Protobuf）、开发计划（M0-M5，约 26 周）与 Git 规范（main/develop/feature/release/hotfix 分支模型、Conventional Commits、SemVer tag、回滚预案）。
- 初始化 Git 仓库：main 分支，首次提交 `docs: 新增...v0.1.0`，打 annotated tag `v0.1.0`；仓库本地身份暂设为 dsh（需用户推送前改为真实邮箱）。
- 项目长期关键事实记录在 `MEMORY.md`。
- 【改版】用户澄清：游戏是**单机**不是联网；且要求 git 首次提交/推送前应向用户索取仓库地址。已重写文档为 v0.2.0：移除服务端/联网社交，改为本地存档体系（原子写入+校验+备份轮换+saveVersion 迁移）；提交 `docs!: 改版为单机离线方案...（v0.2.0）`（9b63f16）+ tag `v0.2.0`；新增 `.gitignore`（排除 .workbuddy/）。remote 仍未配置，等用户提供仓库地址。已把"git 远程操作前先索取仓库地址"记入 MEMORY.md 流程约定。
- 【开工 M0】接手 dsh 开发职责。环境核查：Node 24.20 ✅；git 未装 / winget 异常 / 终端无外网；Cocos 未装（计划装 D 盘）；本目录无 `.git`（v0.2.0 历史提交不在本机）→ git 装好后重新 init。创建 monorepo 骨架（client/battle-core/config/tools/docs/.github）与 commitlint/CI 草案；`开发文档.md` 移入 `docs/`（补 incidents.md）。产品负责人提供远程仓库：https://github.com/rootgrac/sxdproject.git。开工零依赖纯 TS 原型：`battle-core` 确定性战斗内核（9 测 ✅）与 `tools/save-prototype` 存档内核（6 测 ✅，含 v1→v2 迁移 fixtures）。
- 【M0 继续】用户装好 Cocos Dashboard 于 D:\CocosDashboard（v31.3.1）；Creator 3.8.x 仍未下载（需 Dashboard 登录）。git 仍未安装。新增：save-prototype 多槽位 SlotManager（slot1-3 + auto：创建/删除/复制/自动快照/too-new 防覆盖/只读 summary），存档测试增至 13 项全绿；新增 `tools/m0-demo` CLI 演示（`npm run demo`：3v3 战斗回放摘要 + 同种子一致性校验 + 存档损坏自动回退修复）；README 增补 M0 进展速览。
- 【M0 继续2】git / Creator 仍待用户操作（Dashboard 尚无用户数据目录，可能尚未登录）。新增：`config/battle.md`（占位数值登记 v0.1 + 待定项）；`tools/sim` 批量战斗模拟器雏形（1000 场/场景 ×4 对照，4000 场秒级完成；基准我方胜率 95%——占位数值偏强，正式表需平衡）；文档同步 README/tools/MEMORY。
- 【M0 继续3】用户完成 Creator 下载：**Creator 3.8.8** 在 `C:\ProgramData\cocos\editors\Creator\3.8.8`（Dashboard 默认装 C 盘）。用官方 empty-2d 模板初始化 `client/`（package name xiantu-hd；模块裁剪 2D/spine/audio；建 assets/scenes+scripts）。沙箱无法启动 GUI（notepad 对照失败、CocosCreator crashpad 退出），首次导入需用户 Dashboard 打开项目。根目录聚合 `npm test`（22 测）落地并验证全绿。git 仍未装。
- 【M0 继续4】用户完成 Dashboard 首次导入（client 生成 library/temp/项目 uuid）。新增：`tools/sync-battle-core.mjs`（battle-core→client/assets/scripts/battle-core 同步、去 .ts 扩展）；client 存档内核适配层 `framework/save/`（自研同步 SHA-256（FIPS 向量+node:crypto 对拍 ✅）+ KV 抽象 KvSaveStore，行为与 Node 原型对拍）；`tools/client-core-test`（loader hook 解析 Cocos 无扩展 import）9 测全绿；`modules/battle/BattleDemoBoot.ts` 3v3 自动战斗演出（纯代码 UI，待编辑器装配预览）。根 `npm test` 聚合 **31 测全绿**。git 仍未装。
- 【M0 继续5】复检：git 仍未装；编辑器已导入 client 新脚本（battle-core/framework/save meta 已生成 ✅）；BattleDemoBoot/modules 尚无 meta（用户未重开编辑器）、Main 场景未建。BattleDemoBoot.ts 全文自查：语法与调度/渲染逻辑无问题（node 冒烟报错仅为 strip-only 不支持装饰器，非真实错误）。新增 `docs/m0-checklist.md`（M0 DoD 验收跟踪）。
- 【M0 继续6】用户重开编辑器：`BattleDemoBoot.ts.meta` 已生成（11:58，编译导入成功 ✅）；Main 场景未建、预览未做。git 仍未装。CI 真实化：`.github/workflows/ci.yml` 启用零依赖单测门禁（setup-node 24 → root `npm test`），commitlint 步骤以占位注释登记（待 package-lock.json 落地恢复）。
- 【M0 继续7】新增 `client framework/save/save-manager.ts`（§3.5-1 运行时写盘调度：内存单一数据源 + 脏标记合并 + 30s 批量节拍 + flushNow 关键节点即写 + lastSavedAt 维护，时钟注入可测）+ 5 项单测（含合并写盘/未满 30s 不写/无修改不写）。根 `npm test` 聚合 **36 测全绿**。git 仍未装、Main 场景待用户装配。
- 【M0 继续8】开发文档修订 v0.2.1：§5.1/§5.7 远程地址占位文本更正为实际地址（rootgrac/sxdproject.git）+ 修订记录与版本号同步。demo/回归冒烟通过（36 测全绿）。git 仍未装、Main 场景未建（library 停于 11:58）。
- 【M0 继续9】用户已在编辑器创建 **Main.scene**（12:01，含 Canvas + Camera 节点 ✅，scene-2d 根）；**BattleDemoBoot 组件尚未挂载**（场景内无组件 uuid）。编辑器 project.log 无错误。等待用户挂组件 → 预览；git 仍未装。
- 【M0 继续10】复检（Round 9）：git 仍未装；场景 mtime 与 library 均停于 12:01，组件未挂、无预览活动。**全部可离线完成的 M0 工作已完成**，剩余两条关键路径（git init/提交/推送；编辑器挂组件+预览）均依赖用户操作 → 记录阻塞等待用户。
- 【🎉 M0 里程碑】用户确认装配完成并**预览成功显示**（12:04 Main.scene 保存，Demo 节点挂 BattleDemoBoot，uuid 压缩引用 72df0xa02VP+…）。**3v3 战斗 demo 在 Creator 引擎内跑通（占位美术）✅** —— M0 核心验收项达成。剩余：git 安装（尚未装）→ 仓库提交推送 + CI 首跑。
- 【仓库重建】用户安装 Git for Windows 2.55.0（D:\Git）；身份 rootgrac/1830455734@qq.com。`git init -b main` → 首提交 `b168cdc`（83 文件/4969 行，chore: 初始化仓库基线）→ annotated tag `v0.2.1` → `develop` 分支 → remote origin 配置。**推送失败**（沙箱终端无外网 schannel SEC_E_NO_CREDENTIALS）→ 待用户在普通终端执行 push（GCM 弹窗登录），命令已提供。
- 【✅ M0 收官】用户在普通终端执行推送成功（upstream + origin/main + origin/develop + tag v0.2.1 remote-tracking refs 均已确认）。M0 全部验收完成；CI 首跑结果待从 GitHub Actions 页面确认。
- 【M1 进行】多轮推进（详情见 docs/m1-plan.md 与 m1-engine-check.md）：M1-1 导表工具（TSV→JSON+校验 6 测）、M1-2 示例表（unit/skill/skill_effect/stage 入 config/export）、M1-3 配置驱动阵容（setup.ts buildUnits）、M1-4 存档接入（KV SlotManager + GameSession 会话控制器 + BattleGameBoot 可玩 UI + config 同步 client resources）、M1-5 3×3 布阵内核（position/列优先目标/站位加成）、M1-6 技能效果器（damage/heal/buff/debuff × 乘区回合制）、M1-7 养成纯逻辑（等级/境界/战力）、M1-8 主线推进 + 第一章 10 关（demo 打通）、M1-9 不丢档集成用例（多关连推→新会话恢复）。**根 npm test = 77 项全绿**（battle 22 + save 13 + client-core 36 + excel 6）。编辑器已导入全部新脚本与 resources/config JSON（13:56-13:57）。**待产品负责人**：Main.scene 移除 BattleDemoBoot → 挂 BattleGameBoot → 预览 → 刷新续档验收（步骤见 docs/m1-engine-check.md）；git push 保持同步（develop 领先远程多个提交）。
- 【🎉 M1 收官】产品负责人完成装配与验收（14:00 Main.scene 挂 BattleGameBoot、旧组件移除，预览确认），并推送同步 develop（ahead 0）。**M1 核心闭环全部达成**：存档→选档→推图→自动存档（刷新续档通过）；第一章 10 关 + 养成 + 效果器 + 布阵内核就绪。M1 下一阶段：收尾复盘、README 里程碑页、M2 规划（伙伴/装备/经济，开发文档 W8-W12）。
- 【M2 进行】伙伴核心与存档 v3（party）、招贤阁招募（十连保底原创）、装备（穿戴/强化/打造/分解）、背包物品、商店、精英副本每日、存档 v4（daily）、GameSession M2 操作层、修行 Hub UI、表驱动 sim 基线；根 npm test 107 项全绿。产品预览发现两处 bug 已修（loadConfigs 缺 M2 配置表致修行崩溃；视图切换未清 UI 致重叠）。
- 【🎉 M2 收官】产品负责人确认修复后预览正常（14:26 重载），**M2 引擎验收通过**；远程同步（ahead 0）。M2 数值暂按占位运行，拍板清单 docs/m2-balance.md 随时可调。下一步 M3（草案 docs/m3-plan.md）。
